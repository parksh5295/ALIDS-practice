# ALIDS-practice / NIDSGAN stack (Python 3.10+)
# Install: pip install -r requirements.txt
# CPU-only PyTorch (smaller): pip install torch --index-url https://download.pytorch.org/whl/cpu
# then: pip install -r requirements.txt --no-deps  (only if you manage torch separately)

numpy>=1.24.0,<3.0.0
pandas>=2.0.0,<3.0.0
scipy>=1.10.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0

torch>=2.0.0,<3.0.0
tensorboard>=2.14.0,<3.0.0

ConfigArgParse>=1.7.0
PyYAML>=6.0.0
tabulate>=0.9.0
joblib>=1.3.0
